import request from "supertest";
import { app } from "../../app";

const apiPrefix = `/api/v1/integration/cola/promo-list`;

it("returns a 200 status code", async () => {
  // await request(app).get(apiPrefix).expect(200);
});
